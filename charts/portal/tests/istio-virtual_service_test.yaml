suite: virtual service
templates:
  - istio-virtual-service.yaml
values:
  - ../test-values.yaml
tests:
  - it: virtual service match the snapshot
    set:
      global:
        istio:
          enabled: true
    asserts:
      - matchSnapshot: {}
