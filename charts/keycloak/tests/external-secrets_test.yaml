suite: external-secrets
templates:
  - external-secrets.yaml
tests:
  - it: matches the snapshot
    template: external-secrets.yaml
    set:
      externalSecrets:
        enabled: true
    asserts:
      - matchSnapshot: {}
