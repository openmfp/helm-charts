suite: Test the Template files  with test values
values:
- ../test_values.yaml
release:
  name: openmfp
  namespace: openmfp-system
chart:
  appVersion: 1.0.0
templates:
  - deploy.yaml
  - virtual-workspaces-configmap.yaml
tests:
- it: match the Snapshot
  asserts:
  - matchSnapshot: {}
